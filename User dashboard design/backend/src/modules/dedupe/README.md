## Dedupe Module

Use this module for the most complex logic after schema checks.

Responsibilities:
- exact duplicate matching
- configurable dedupe keys
- future fuzzy matching
- duplicate confidence scoring

Expected next files:
- `exact-dedupe-service.mjs`
- `fuzzy-dedupe-service.mjs`
