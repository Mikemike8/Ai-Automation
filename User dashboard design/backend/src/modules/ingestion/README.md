## Ingestion Module

Own this module when backend work expands beyond pasted CSV strings.

Responsibilities:
- CSV upload parsing
- delimiter/header detection
- header alias mapping
- row normalization
- malformed row capture

Expected next file:
- `csv-ingestion-service.mjs`
